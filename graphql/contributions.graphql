query Contributions($login: String!) {
  user(login: $login) {
    pullRequests(first: 5, states: MERGED) {
      nodes {
        title
        permalink
        repository {
          name
          owner {
            __typename
            login
            avatarUrl
          }
        }
      }
      pageInfo {
        endCursor
        hasNextPage
      }
    }
  }
}
